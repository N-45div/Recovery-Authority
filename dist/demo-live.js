// @bun
import{spawnSync}from"child_process";import{existsSync}from"fs";import{mkdir,writeFile}from"fs/promises";import{homedir}from"os";import{dirname,join,resolve}from"path";import{fileURLToPath}from"url";var root=resolve(process.env.PLUGIN_ROOT??join(dirname(fileURLToPath(import.meta.url)),"..")),timestamp=new Date().toISOString().replaceAll(/[^0-9TZ]/g,""),session=process.env.RECOVERY_AUTHORITY_TMUX_SESSION??`recovery-live-${timestamp}`;if(!session.match(/^[A-Za-z0-9_-]+$/))throw Error("RECOVERY_AUTHORITY_TMUX_SESSION may contain only letters, numbers, underscores, and hyphens");var sessionRoot=resolve(process.env.RECOVERY_AUTHORITY_LIVE_ROOT??join(homedir(),".local","share","recovery-authority","live-sessions",timestamp)),dataDir=join(sessionRoot,"data"),keyDir=join(sessionRoot,"keys"),demoFixture=join(root,".internal","live-demo","customer-data.txt"),tui=join(root,"target","release","recovery-authority"),checkOnly=process.argv.includes("--check"),terminalWidth=String(Math.max(process.stdout.columns??160,120)),terminalHeight=String(Math.max(process.stdout.rows??45,36)),requiredForwardedEnvironment=["RECOVERY_AUTHORITY_MCP_SOCKET","RECOVERY_AUTHORITY_AUDIT_SOCKET","RECOVERY_AUTHORITY_SANDBOX","RECOVERY_AUTHORITY_KEY_DIR","RECOVERY_AUTHORITY_DATA_DIR"];function run(command,args,options={}){let result=spawnSync(command,args,{cwd:root,encoding:options.inherit?void 0:"utf8",stdio:options.inherit?"inherit":"pipe",env:options.env??process.env});if(result.error)throw result.error;if(result.status!==0&&!options.allowFailure){let detail=typeof result.stderr==="string"?result.stderr.trim():"";throw Error(`${command} failed with status ${result.status}${detail?`: ${detail}`:""}`)}return result}function requireCommand(command){if(!Bun.which(command))throw Error(`Live demo requires '${command}' on PATH`)}function shellQuote(value){return`'${value.replaceAll("'","'\\''")}'`}function shellCommand(parts){return parts.map(shellQuote).join(" ")}async function assertMcpForwarding(pluginRoot,label){let configPath=join(pluginRoot,".mcp.json"),mcp=(await Bun.file(configPath).json())?.mcpServers?.["recovery-authority"],forwarded=mcp?.env_vars,missing=requiredForwardedEnvironment.filter((name)=>!Array.isArray(forwarded)||!forwarded.includes(name));if(missing.length>0)throw Error([`${label} does not forward the host authority boundary into its MCP worker.`,`Missing from ${configPath}: ${missing.join(", ")}`,"Refresh the marketplace and reinstall Recovery Authority before starting a live demo."].join(`
`));if(JSON.stringify(mcp?.args??[]).includes("${PLUGIN_ROOT}"))throw Error([`${label} relies on unsupported PLUGIN_ROOT argument interpolation.`,`Codex passes the placeholder literally and the MCP worker cannot start: ${configPath}`,"Refresh the marketplace and reinstall Recovery Authority before starting a live demo."].join(`
`));if(mcp?.cwd!=="."||mcp?.args?.[0]!=="./dist/mcp.js")throw Error([`${label} does not use the portable plugin-relative MCP launcher.`,`Expected cwd '.' and entry './dist/mcp.js' in ${configPath}`,"Refresh the marketplace and reinstall Recovery Authority before starting a live demo."].join(`
`))}function assertMcpHandshake(pluginRoot){run("node",[join(root,"scripts","smoke-mcp.mjs")],{env:{...process.env,PLUGIN_UNDER_TEST:pluginRoot}})}for(let command of["bun","cargo","codex","tmux","bwrap"])requireCommand(command);if(!existsSync(tui))throw Error("Release TUI is missing. Run: cargo build --release -p recovery-authority");var pluginList=run("codex",["plugin","list"]),pluginMatch=String(pluginList.stdout).match(/^recovery-authority@recovery-authority\s+installed, enabled\s+(\S+)\s+(.+)$/m);if(!pluginMatch)throw Error(["Recovery Authority is not installed and enabled in Codex.","Run:","  codex plugin marketplace add N-45div/Recovery-Authority --ref main","  codex plugin add recovery-authority@recovery-authority"].join(`
`));await assertMcpForwarding(root,"Repository MCP configuration");var codexHome=resolve(process.env.CODEX_HOME??join(homedir(),".codex")),installedPluginRoot=join(codexHome,"plugins","cache","recovery-authority","recovery-authority",pluginMatch[1]);if(!existsSync(installedPluginRoot))throw Error(["Codex reports Recovery Authority as installed, but its installed cache is missing.",`Expected: ${installedPluginRoot}`,`Marketplace source: ${pluginMatch[2].trim()}`,"Refresh the marketplace and reinstall Recovery Authority."].join(`
`));await assertMcpForwarding(installedPluginRoot,"Installed Recovery Authority plugin");var mcpRegistration=String(run("codex",["mcp","get","recovery-authority"]).stdout),missingRegistration=requiredForwardedEnvironment.filter((name)=>!mcpRegistration.includes(name));if(!mcpRegistration.includes("transport: stdio")||missingRegistration.length>0)throw Error(["Codex does not expose the expected native Recovery Authority MCP registration.",`Missing forwarded environment entries: ${missingRegistration.join(", ")||"none"}`,"Refresh and reinstall the plugin before starting a live demo."].join(`
`));assertMcpHandshake(installedPluginRoot);if(checkOnly)process.stdout.write(["Recovery Authority live-demo preflight passed.",`Workspace: ${root}`,`Release TUI: ${tui}`,"Plugin: installed and enabled","Host authority socket forwarding: verified","Native Codex MCP handshake: verified","tmux and bubblewrap: available",`tmux recording size: ${terminalWidth}x${terminalHeight}`,""].join(`
`)),process.exit(0);await mkdir(dataDir,{recursive:!0,mode:448});await mkdir(keyDir,{recursive:!0,mode:448});await mkdir(dirname(demoFixture),{recursive:!0});await writeFile(demoFixture,`Ada
Grace
`,{mode:384});var codexCommand=shellCommand(["env","RECOVERY_AUTHORITY_APPROVAL_ENV_READY=1","bun","run","sandbox","--workspace",root,"--data-dir",dataDir,"--key-dir",keyDir,"--","codex","--no-alt-screen"]),tuiCommand=shellCommand([tui,"tui","--data-dir",dataDir]),approvalCommand=shellCommand(["env",`RECOVERY_AUTHORITY_DATA_DIR=${dataDir}`,`RECOVERY_AUTHORITY_KEY_DIR=${keyDir}`,"PS1=[human-approval] \\w \\$ ","bash","--noprofile","--norc"]),agentPane=String(run("tmux",["new-session","-d","-P","-F","#{pane_id}","-x",terminalWidth,"-y",terminalHeight,"-s",session,"-n","agent","-c",root,codexCommand]).stdout).trim();try{let approvalPane=String(run("tmux",["split-window","-v","-l","24%","-P","-F","#{pane_id}","-t",agentPane,"-c",root,approvalCommand]).stdout).trim();run("tmux",["select-pane","-t",agentPane,"-T","Codex agent"]),run("tmux",["select-pane","-t",approvalPane,"-T","Human approval"]),run("tmux",["select-pane","-t",agentPane]),run("tmux",["new-window","-d","-t",`${session}:`,"-n","mission","-c",root,tuiCommand]),run("tmux",["set-option","-t",session,"mouse","on"]),run("tmux",["set-window-option","-t",session,"pane-border-status","top"]),run("tmux",["set-option","-t",session,"status-position","top"]),run("tmux",["set-option","-t",session,"status-style","bg=black,fg=white"]),run("tmux",["set-option","-t",session,"window-status-current-style","bg=green,fg=black,bold"]),run("tmux",["select-window","-t",`${session}:agent`])}catch(error){throw run("tmux",["kill-session","-t",session],{allowFailure:!0}),error}process.stdout.write([`Recovery Authority live session: ${session}`,`Evidence ledger: ${dataDir}`,`Fresh demo fixture: ${demoFixture}`,"","agent: sandboxed Codex plus separate human approval terminal","mission: live Rust TUI over the same host-owned ledger","","Use Ctrl-b n to switch windows. In the TUI use 1-8 and q.",""].join(`
`));var attachArgs=process.env.TMUX?["switch-client","-t",session]:["attach-session","-t",session],attached=run("tmux",attachArgs,{inherit:!0,allowFailure:!0});process.exit(attached.status??0);
